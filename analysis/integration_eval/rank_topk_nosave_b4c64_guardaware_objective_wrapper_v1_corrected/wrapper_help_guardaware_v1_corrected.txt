usage: probe_policy_rank_topk_protected_nosave_b4c64_current_best.py
       [-h] [--dataset DATASET] [--anchor-snapshots ANCHOR_SNAPSHOTS]
       [--init-checkpoint INIT_CHECKPOINT]
       [--reference-checkpoint REFERENCE_CHECKPOINT] [--board-size BOARD_SIZE]
       [--win-length WIN_LENGTH] [--channels CHANNELS] [--blocks BLOCKS]
       [--epochs EPOCHS] [--lr LR] [--margin MARGIN] [--ce-weight CE_WEIGHT]
       [--pair-weight PAIR_WEIGHT] [--worst-weight WORST_WEIGHT]
       [--anchor-kl-weight ANCHOR_KL_WEIGHT]
       [--hard-guard-reference-kl-weight HARD_GUARD_REFERENCE_KL_WEIGHT]
       [--hard-guard-target-ce-weight HARD_GUARD_TARGET_CE_WEIGHT]
       [--hard-guard-beats-weight HARD_GUARD_BEATS_WEIGHT]
       [--hard-guard-beats-margin HARD_GUARD_BEATS_MARGIN]
       [--weight-decay WEIGHT_DECAY] [--seed SEED] [--print-every PRINT_EVERY]
       [--out-csv OUT_CSV] [--out-report OUT_REPORT]
       [--out-row-csv OUT_ROW_CSV] [--out-hard-guard-csv OUT_HARD_GUARD_CSV]

b4c64/current-best-safe no-save protected rank/top-k objective ablation
wrapper. Optimizer runs in memory only; no checkpoint is saved.

options:
  -h, --help            show this help message and exit
  --dataset DATASET
  --anchor-snapshots ANCHOR_SNAPSHOTS
  --init-checkpoint INIT_CHECKPOINT
  --reference-checkpoint REFERENCE_CHECKPOINT
  --board-size BOARD_SIZE
  --win-length WIN_LENGTH
  --channels CHANNELS
  --blocks BLOCKS
  --epochs EPOCHS
  --lr LR
  --margin MARGIN
  --ce-weight CE_WEIGHT
  --pair-weight PAIR_WEIGHT
  --worst-weight WORST_WEIGHT
  --anchor-kl-weight ANCHOR_KL_WEIGHT
  --hard-guard-reference-kl-weight HARD_GUARD_REFERENCE_KL_WEIGHT
                        Guard-aware v1: KL weight preserving current_best
                        distribution on hard_guard_samples.
  --hard-guard-target-ce-weight HARD_GUARD_TARGET_CE_WEIGHT
                        Guard-aware v1: target CE weight on
                        hard_guard_samples.
  --hard-guard-beats-weight HARD_GUARD_BEATS_WEIGHT
                        Guard-aware v1: beats-worst hinge weight on hard guard
                        rows that previously beat suppressors.
  --hard-guard-beats-margin HARD_GUARD_BEATS_MARGIN
                        Guard-aware v1: margin for hard-guard beats
                        preservation; default 0 preserves target above
                        suppressors.
  --weight-decay WEIGHT_DECAY
  --seed SEED
  --print-every PRINT_EVERY
  --out-csv OUT_CSV
  --out-report OUT_REPORT
  --out-row-csv OUT_ROW_CSV
                        Optional per-row before/after diagnostics CSV. No
                        checkpoints are saved.
  --out-hard-guard-csv OUT_HARD_GUARD_CSV
                        Optional hard-guard row verdict CSV when the dataset
                        provides hard_guard_samples. No checkpoints are saved.
